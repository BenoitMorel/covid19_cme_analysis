

Usage:
Install required tools with
./setup.sh

Run the pipeline by calling the scripts in the pipeline directory (only works on hits internal cluster for now)

Requirements:
bison, flex, MPI, ete3

